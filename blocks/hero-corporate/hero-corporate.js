export default function decorate() {
  // Hero content is rendered as-is from the authored document.
  // The section background-image is set via section-metadata ("background" key)
  // and handled by decorateSections in scripts.js.
}
